# CLI 拆包实施计划（阶段 4：main.py → src/cli/）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `main.py` 从 580 行单体拆成 `src/cli/` 包（每子命令一模块），`main.py` 只留入口 + bootstrap doctor + 分派，顶层 import 减到最轻（为阶段 6 启动优化铺路）。

**Architecture:** 新建 `src/cli/`：`parser.py`（argparse）、`common.py`（outcome 呈现共享函数）、`daily.py` / `weekly.py` / `monthly.py`（报告 handler，承接 Plan 2 已收窄的 ReportRunner 调用）、`doctor.py`（run_doctor_cmd）、`list_reports.py`。`main.py` 只保留 `_run_bootstrap_doctor`（rich 不可用时的轻量诊断，不可丢）与 `main()` 分派，其余全部外迁。

**Tech Stack:** Python 3.13、argparse、pytest、uv run。

**前置：** Plan 2（ReportRunner）已完成 —— `main.py` 报告命令已改为 `ReportRunner.run` 调用、`_present_report_outcome` 已存在于 `main.py`；本 plan 只做**位置迁移与 import 收口**，不改变行为。

## Global Constraints

- 前置：`uv run pytest` 全绿基线（≥237 passed）。
- 修改范围：`main.py`、`src/cli/`（新建）、`tests/test_main.py`、`tests/test_cli_split.py`（新建）；**禁止改** 其他 `src/**`、`rust/`、`templates/`。
- CLI 行为 1:1 等价：参数、退出码（0/1/130）、提示文案、Markdown 预览、`--help` 输出不变；`_run_bootstrap_doctor` 语义保留。
- 每 Task 结束 `uv run pytest` 全绿。
- 本阶段**不做** import 延迟优化（阶段 6 做）；只保证 `main.py` 顶层不新增重依赖 import。

---

### Task 1: 迁移 argparse 到 src/cli/parser.py

**Files:**
- Create: `src/cli/__init__.py`、`src/cli/parser.py`
- Modify: `tests/test_cli_split.py`（新建，断言 parser 等价）
- Modify: `main.py`（`build_parser` 改为从 `src.cli.parser` 导入）

**Interfaces:**
- Consumes: 现有 `main.build_parser`（参数定义，逐字搬）
- Produces: `src.cli.parser.build_parser()`；`src.cli` 包可导入；main.py 复用同一 parser

- [ ] **Step 1: 写失败测试（parser 参数等价）**

创建 `tests/test_cli_split.py`：
```python
"""src/cli 拆分后的行为等价性测试。"""
from __future__ import annotations

import pytest

from src.cli.parser import build_parser


@pytest.mark.parametrize(
    ("argv", "subcommand"),
    [
        (["daily", "-i", "x"], "daily"),
        (["daily", "--no-save"], "daily"),
        (["weekly", "2026-W05", "--source", "scan"], "weekly"),
        (["monthly", "2026-01", "--source", "db"], "monthly"),
        (["list"], "list"),
        (["doctor"], "doctor"),
        (["check-config"], "doctor"),
    ],
)
def test_parser_accepts_equivalent_commands(argv, subcommand):
    assert build_parser().parse_args(argv).subcommand == subcommand


def test_parser_weekly_requires_source():
    with pytest.raises(SystemExit):
        build_parser().parse_args(["weekly"])


def test_parser_preserves_daily_flags():
    args = build_parser().parse_args(["daily", "--no-save", "--date", "2026-02-05"])
    assert args.no_save is True
    assert args.date == "2026-02-05"
```

- [ ] **Step 2: 运行确认失败**

Run: `uv run pytest tests/test_cli_split.py -q`
Expected: FAIL（`ModuleNotFoundError: src.cli.parser`）。

- [ ] **Step 3: 创建 src/cli/ 包与 parser.py**

创建 `src/cli/__init__.py`：
```python
"""CLI 子命令模块；main.py 只做入口分派。"""
```

创建 `src/cli/parser.py` —— 把 `main.py` 现有 `build_parser` 函数体**逐字搬移**（含 daily/weekly/monthly/list/doctor/check-config 全部子命令与参数定义），并在顶部加：
```python
"""argparse 子命令定义。"""

from __future__ import annotations

import argparse


def build_parser() -> argparse.ArgumentParser:
    """构建子命令 CLI 解析器（从 main.py 迁入，参数保持 1:1 等价）。"""
    parser = argparse.ArgumentParser(description="审计日报生成器 v5.0")
    # …（粘贴 main.py 中 build_parser 的完整函数体）
    return parser
```

- [ ] **Step 4: main.py 改为复用**

在 `main.py` 删除本地 `build_parser` 定义，改为：
```python
from src.cli.parser import build_parser
```
（放在 `main.py` 原有 import 区；本阶段暂不调整 import 位置。）

- [ ] **Step 5: 运行确认通过 + 全量**

Run: `uv run pytest tests/test_cli_split.py tests/test_main.py -q`
Expected: PASS（原 parser 相关测试继续通过）。
Run: `uv run pytest`
Expected: 全绿。

- [ ] **Step 6: Commit**

```bash
git add src/cli/parser.py src/cli/__init__.py tests/test_cli_split.py main.py
git commit -m "refactor: move argparse builder into src/cli/parser"
```

---

### Task 2: 迁移 list 与 doctor 子命令

**Files:**
- Create: `src/cli/list_reports.py`、`src/cli/doctor.py`
- Modify: `src/cli/common.py`（新建，放共享呈现）
- Modify: `tests/test_cli_split.py`（追加 list/doctor 断言）
- Modify: `main.py`（改为从 src.cli 导入）

**Interfaces:**
- Consumes: 现有 `main.list_reports`、`main.run_doctor_cmd`、`main._run_bootstrap_doctor`、`main.collect_healthcheck`、`main.console`
- Produces: `src.cli.list_reports.list_reports(console)`、`src.cli.doctor.run_doctor_cmd(*, console, collect, strict)`、`src.cli.doctor.run_bootstrap_doctor(strict)`；main.py 不再定义它们

- [ ] **Step 1: 写失败测试**

在 `tests/test_cli_split.py` 追加：
```python
from src.cli.list_reports import list_reports
from src.cli.doctor import run_doctor_cmd


def test_list_reports_shows_empty_hint(monkeypatch):
    printed: list[str] = []
    console = type("C", (), {"print": lambda self, *a, **k: printed.append(a[0])})()
    store = type("S", (), {"list_all_reports": lambda self: []})()

    list_reports(console, store=store)

    assert any("暂无日报数据" in text for text in printed)


def test_run_doctor_cmd_passes_strict(monkeypatch):
    printed: list[str] = []
    console = type("C", (), {"print": lambda self, *a, **k: printed.append(a[0])})()
    seen = []

    def collect(*, strict=False):
        seen.append(strict)
        return type(
            "R",
            (),
            {
                "info": {"LLM Provider": "deepseek"},
                "warnings": ["w"],
                "errors": [],
            },
        )()

    ok = run_doctor_cmd(console=console, collect=collect, strict=True)

    assert ok is True
    assert seen == [True]
    assert any("所有检查通过" in text for text in printed)
```

- [ ] **Step 2: 运行确认失败**

Run: `uv run pytest tests/test_cli_split.py -q`
Expected: FAIL（`ModuleNotFoundError`）。

- [ ] **Step 3: 创建 list_reports.py 与 doctor.py**

创建 `src/cli/list_reports.py`（把 `main.py` 现有 `list_reports` 函数体搬入，`SQLiteStore` 改为注入参数、`console` 注入参数）：
```python
"""list 子命令：列出已有日报。"""

from __future__ import annotations

from collections import defaultdict
from typing import Any


def list_reports(console: Any, store: Any) -> None:
    """列出已有日报（按月分组、倒序）。"""
    console.print("\n[bold green]===== 已有日报列表 =====[/bold green]\n")
    dates = store.list_all_reports()
    if not dates:
        console.print("[yellow]暂无日报数据[/yellow]")
        return

    by_month: dict[str, list[str]] = defaultdict(list)
    for date_str in dates:
        by_month[date_str[:7]].append(date_str)

    for month in sorted(by_month.keys(), reverse=True):
        console.print(f"[bold cyan]{month}[/bold cyan]")
        for date_str in sorted(by_month[month], reverse=True):
            console.print(f"  - {date_str}")
        console.print()

    console.print(f"[green]共 {len(dates)} 份日报[/green]")
```

创建 `src/cli/doctor.py`（把 `main.py` 现有 `run_doctor_cmd` 与 `_run_bootstrap_doctor` 搬入，依赖注入 console/collect）：
```python
"""doctor 子命令：环境检查与轻量 bootstrap 诊断。"""

from __future__ import annotations

import sys
from typing import Any


def run_doctor_cmd(*, console: Any, collect: Any, strict: bool = False) -> bool:
    """检查运行环境和配置。"""
    console.print("\n[bold green]===== 环境检查 =====[/bold green]\n")
    result = collect(strict=strict)

    if result.info:
        console.print("[bold cyan]配置概览[/bold cyan]")
        for label, value in result.info.items():
            console.print(f"  - {label}: {value}")
    if result.warnings:
        console.print("\n[yellow]警告:[/yellow]")
        for message in result.warnings:
            console.print(f"  [!] {message}")
    if result.errors:
        console.print("\n[red]错误:[/red]")
        for message in result.errors:
            console.print(f"  [X] {message}")
        console.print("\n[red]环境检查失败，请修复上述问题[/red]")
        return False

    console.print("\n[green]所有检查通过，可以正常使用[/green]")
    return True


def run_bootstrap_doctor(*, strict: bool = False) -> int:
    """在 rich/业务依赖可用前执行的轻量 doctor；返回进程退出码。

    语义与迁移前一致：优先尝试完整 healthcheck，缺模块/异常时给出脱敏诊断。
    """
    print("\n===== 环境检查 =====\n")
    try:
        from src.core.healthcheck import collect_healthcheck as collect

        result = collect(strict=True) if strict else collect()
    except ModuleNotFoundError as exc:
        missing_module = exc.name or "未知模块"
        print("错误:")
        print(f"  [X] doctor 启动依赖缺失: {missing_module}")
        print("\n请先 uv sync 后重试")
        return 1
    except Exception as exc:
        print("错误:")
        print(
            f"  [X] doctor 无法启动 ({type(exc).__name__})；"
            "请检查依赖与本机配置格式"
        )
        return 1

    if result.info:
        print("配置概览")
        for label, value in result.info.items():
            print(f"  - {label}: {value}")
    if result.warnings:
        print("\n警告:")
        for message in result.warnings:
            print(f"  [!] {message}")
    if result.errors:
        print("\n错误:")
        for message in result.errors:
            print(f"  [X] {message}")
        print("\n环境检查失败，请修复上述问题")
        return 1

    print("\n所有检查通过，可以正常使用")
    return 0
```
> 保留 `run_bootstrap_doctor` 里的"缺模块时提示先 `uv sync`"，是对 Plan 1 迁移的文案更新；其余语义与原有 `_run_bootstrap_doctor` 一致。

- [ ] **Step 4: main.py 改为复用**

在 `main.py`：
- 删除 `run_doctor_cmd` 与 `_run_bootstrap_doctor` 函数体；
- 顶部 `import sys` 保留；`main()` 内分派 `doctor` 时改调 `src.cli.doctor.run_doctor_cmd`；
- bootstrap 分支（`sys.argv[1] in {"doctor", "check-config"}` 的顶层提前退出）改调 `src.cli.doctor.run_bootstrap_doctor`；
- 其余保持。

- [ ] **Step 5: 运行确认通过 + 全量**

Run: `uv run pytest tests/test_cli_split.py tests/test_main.py -q`
Expected: PASS（含原 `test_run_doctor_cmd_uses_healthcheck`、`test_main_returns_doctor_status`、bootstrap 子进程测试）。
Run: `uv run pytest`
Expected: 全绿。

- [ ] **Step 6: Commit**

```bash
git add src/cli/list_reports.py src/cli/doctor.py tests/test_cli_split.py main.py
git commit -m "refactor: move list and doctor subcommands into src/cli"
```

---

### Task 3: 迁移报告命令 handler 到 src/cli/{daily,weekly,monthly}.py

**Files:**
- Create: `src/cli/common.py`（`_present_report_outcome` 迁入并参数化 console/markdown）
- Create: `src/cli/daily.py`、`src/cli/weekly.py`、`src/cli/monthly.py`
- Modify: `tests/test_cli_split.py`（追加报告 handler 映射断言）
- Modify: `main.py`（改为从 src.cli 导入报告 handler）

**Interfaces:**
- Consumes: Plan 2 的 `ReportRunner` / request 类型 / `_present_report_outcome`（当前在 main.py）；现有 `main.console`、`main.Markdown`、`main.Progress`
- Produces: `src.cli.daily.generate_daily_report(args, *, console, runner_factory, markdown)` 等三个 handler；`src.cli.common.present_report_outcome(outcome, *, label, console, markdown) -> bool`

- [ ] **Step 1: 写失败测试**

在 `tests/test_cli_split.py` 追加：
```python
from src.cli.daily import generate_daily_report
from src.cli.weekly import generate_weekly_report_cmd
from src.cli.monthly import generate_monthly_report_cmd


def _success_outcome(**overrides):
    from src.services.report_runner.outcomes import ReportRunSuccess

    values = dict(
        status="ok", mode="daily", source="scan", markdown="# 日报",
        report=None, publication=None, warnings=[], source_evidence=None,
        period=None,
    )
    values.update(overrides)
    return ReportRunSuccess(**values)


def test_daily_handler_maps_namespace_to_request(monkeypatch):
    from argparse import Namespace

    captured: list = []

    class StubRunner:
        def run(self, request):
            captured.append(request)
            return _success_outcome()

    printed: list[str] = []
    console = type("C", (), {"print": lambda self, *a, **k: printed.append(a[0])})()
    runner_factory = lambda: StubRunner()

    ok = generate_daily_report(
        Namespace(input="今天工作", no_save=True, date="2026-05-20"),
        console=console,
        runner_factory=runner_factory,
        markdown=lambda text: text,
    )

    assert ok is True
    req = captured[0]
    assert req.user_input == "今天工作"
    assert req.save is False
    assert req.report_date_override == "2026-05-20"
    assert any("日报预览" in text for text in printed)


def test_weekly_handler_maps_source_and_week(monkeypatch):
    from argparse import Namespace

    captured: list = []

    class StubRunner:
        def run(self, request):
            captured.append(request)
            return _success_outcome(mode="weekly")

    console = type("C", (), {"print": lambda self, *a, **k: None})()
    ok = generate_weekly_report_cmd(
        Namespace(week="2026-W05", source="db", input=None, no_save=False),
        console=console,
        runner_factory=lambda: StubRunner(),
        markdown=lambda text: text,
    )

    assert ok is True
    assert captured[0].source == "db"
    assert captured[0].week_label == "2026-W05"
    assert captured[0].save is True


def test_monthly_handler_maps_year_month(monkeypatch):
    from argparse import Namespace

    captured: list = []

    class StubRunner:
        def run(self, request):
            captured.append(request)
            return _success_outcome(mode="monthly")

    console = type("C", (), {"print": lambda self, *a, **k: None})()
    ok = generate_monthly_report_cmd(
        Namespace(month="2026-05", source="scan", input="补充", no_save=True),
        console=console,
        runner_factory=lambda: StubRunner(),
        markdown=lambda text: text,
    )

    assert ok is True
    assert captured[0].source == "scan"
    assert captured[0].year_month == "2026-05"
    assert captured[0].save is False
    assert captured[0].supplemental_input == "补充"
```

- [ ] **Step 2: 运行确认失败**

Run: `uv run pytest tests/test_cli_split.py -q`
Expected: FAIL（`ModuleNotFoundError: src.cli.daily`）。

- [ ] **Step 3: 创建 common.py 与三个报告 handler**

创建 `src/cli/common.py`（把 `main.py` 的 `_present_report_outcome` 迁入并参数化）：
```python
"""CLI 共享呈现：把 ReportRunOutcome 映射为提示、预览与退出码语义。"""

from __future__ import annotations

from typing import Any

from src.services.report_runner.outcomes import ReportRunFailure


def present_report_outcome(
    outcome: Any, *, label: str, console: Any, markdown: Any
) -> bool:
    """返回 True 表示成功；False 表示预期失败（对应退出码 1）。"""
    if isinstance(outcome, ReportRunFailure):
        if outcome.error is not None:
            console.print(f"[red]错误: {outcome.error.message}[/red]")
        return False

    if outcome.source == "scan" and outcome.source_evidence is not None:
        console.print(
            "[green]✓[/green] 扫描完成: "
            f"{outcome.source_evidence.success_count}/"
            f"{outcome.source_evidence.source_file_count} 个文件\n"
        )
    for warning in outcome.warnings:
        console.print(f"[yellow]![/yellow] 文件上下文不完整: {warning.message}\n")
    console.print("[green]✓[/green] 报告生成成功\n")
    if outcome.publication is not None and outcome.publication.requested:
        console.print("[green]✓[/green] 报告已保存\n")
    console.print(f"[bold cyan]===== {label}预览 =====[/bold cyan]\n")
    console.print(markdown(outcome.markdown))
    return True
```

创建 `src/cli/daily.py`（把 `main.py` 现有 `generate_daily_report` 迁入，依赖注入）：
```python
"""daily 子命令。"""

from __future__ import annotations

from datetime import date
from typing import Any, Callable

from src.core.config import config
from src.core.llm import LLMClient
from src.core.logger import setup_logger
from src.services.context_scheduler import ContextScheduler
from src.services.report_gen import ReportGenerator
from src.services.report_runner.input_adapter import ConsoleDailyInputAdapter
from src.services.report_runner.model_port import LLMModelPort
from src.services.report_runner.requests import DailyReportRunRequest
from src.services.sqlite_store import SQLiteStore
from src.services.report_runner.runner import ReportRunner

from .common import present_report_outcome

logger = setup_logger()


def generate_daily_report(
    args: Any,
    *,
    console: Any,
    runner_factory: Callable[[], ReportRunner] | None = None,
    markdown: Any = None,
) -> bool:
    """生成日报。"""
    console.print("\n[bold green]===== 审计日报生成器 v5.0 =====[/bold green]\n")
    from rich.markdown import Markdown

    runner = runner_factory() if runner_factory else _default_runner(console)
    outcome = runner.run(
        DailyReportRunRequest(
            as_of_date=date.today(),
            save=not args.no_save,
            user_input=args.input,
            report_date_override=args.date,
        )
    )
    return present_report_outcome(
        outcome,
        label="日报",
        console=console,
        markdown=markdown or Markdown,
    )


def _default_runner(console: Any) -> ReportRunner:
    return ReportRunner(
        scheduler=ContextScheduler(),
        store=SQLiteStore(),
        renderer=ReportGenerator(),
        model_port=LLMModelPort(client_factory=LLMClient),
        daily_input=ConsoleDailyInputAdapter(console=console),
    )
```

创建 `src/cli/weekly.py` 与 `src/cli/monthly.py`：结构同 daily，request 构造分别为：
```python
WeeklyReportRunRequest(
    as_of_date=date.today(),
    source=args.source,
    save=not args.no_save,
    week_label=args.week,
    supplemental_input=args.input,
)
```
```python
MonthlyReportRunRequest(
    as_of_date=date.today(),
    source=args.source,
    save=not args.no_save,
    year_month=args.month,
    supplemental_input=args.input,
)
```
呈现 label 分别为 "周报" / "月报"。
> 说明：weekly/monthly 的 `week_label`/`year_month` 为 None 时由 ReportRunner 按 `as_of_date` 解析（Plan 2 的 `resolve_weekly_period`/`resolve_monthly_period`），与现 CLI 缺省语义一致。旧的 `parse_week_label`/`get_month_date_range` 校验失败路径现在落在 ReportRunner 的 `INVALID_WEEK`/`INVALID_MONTH`（退出码 1），由 `present_report_outcome` 打印 `error.message`。

- [ ] **Step 4: main.py 改为复用**

在 `main.py`：
- 删除 `generate_daily_report` / `generate_weekly_report_cmd` / `generate_monthly_report_cmd` / `_present_report_outcome` / `_accept_context_result` / `get_user_input` 函数体；
- `main()` 分派改为：
```python
case "daily":
    from src.cli.daily import generate_daily_report
    return 0 if generate_daily_report(args, console=console) else 1
case "weekly":
    from src.cli.weekly import generate_weekly_report_cmd
    return 0 if generate_weekly_report_cmd(args, console=console) else 1
case "monthly":
    from src.cli.monthly import generate_monthly_report_cmd
    return 0 if generate_monthly_report_cmd(args, console=console) else 1
case "list":
    from src.cli.list_reports import list_reports
    from src.services.sqlite_store import SQLiteStore
    list_reports(console, store=SQLiteStore())
    return 0
case "doctor":
    from src.cli.doctor import run_doctor_cmd
    from src.core.healthcheck import collect_healthcheck
    return 0 if run_doctor_cmd(console=console, collect=collect_healthcheck, strict=args.strict) else 1
```
- `main.py` 顶层删除不再使用的 import（rich.Console 仍需用于 console 实例，保留；`Markdown` 等移到各 cli 模块）。

- [ ] **Step 5: 运行确认通过 + 全量**

Run: `uv run pytest tests/test_cli_split.py tests/test_main.py -q`
Expected: PASS。
Run: `uv run pytest`
Expected: 全绿。
Run: `uv run python main.py --help`，确认 `usage:` 与参数列表不变（退出码 0）。

- [ ] **Step 6: Commit**

```bash
git add src/cli/common.py src/cli/daily.py src/cli/weekly.py src/cli/monthly.py tests/test_cli_split.py main.py
git commit -m "refactor: move report command handlers into src/cli"
```

---

### Task 4: main.py 收尾为纯入口 + 顶层轻量 import

**Files:**
- Modify: `main.py`（顶层只保留 argparse/sys；console 在 `main()` 内延迟构造）

**Interfaces:**
- Consumes: Task 1–3 后的 `src/cli` 全部 handler
- Produces: `main.py` 顶层不再 import rich 与 src 业务栈（阶段 6 启动优化的结构前提）；`main()` 分派逻辑不变

- [ ] **Step 1: 重构 main.py 顶层**

`main.py` 改为：
```python
"""审计日报生成器 v5.0 — 主入口（子命令逻辑见 src/cli/）。"""

import argparse
import sys

# doctor 必须能在 Rich、业务依赖或文件日志不可用时先给出诊断。
if sys.platform == "win32":
    sys.stdout.reconfigure(encoding="utf-8")
    sys.stderr.reconfigure(encoding="utf-8")

from src.cli.doctor import run_bootstrap_doctor


def main() -> int:
    """主函数。"""
    from src.cli.parser import build_parser
    from rich.console import Console

    parser = build_parser()
    args = parser.parse_args()

    if not args.subcommand:
        parser.print_help()
        return 0

    console = Console()
    try:
        match args.subcommand:
            case "daily":
                from src.cli.daily import generate_daily_report
                return 0 if generate_daily_report(args, console=console) else 1
            case "weekly":
                from src.cli.weekly import generate_weekly_report_cmd
                return 0 if generate_weekly_report_cmd(args, console=console) else 1
            case "monthly":
                from src.cli.monthly import generate_monthly_report_cmd
                return 0 if generate_monthly_report_cmd(args, console=console) else 1
            case "list":
                from src.cli.list_reports import list_reports
                from src.services.sqlite_store import SQLiteStore
                list_reports(console, store=SQLiteStore())
                return 0
            case "doctor":
                from src.cli.doctor import run_doctor_cmd
                from src.core.healthcheck import collect_healthcheck
                return 0 if run_doctor_cmd(console=console, collect=collect_healthcheck, strict=args.strict) else 1
    except KeyboardInterrupt:
        console.print("\n[yellow]操作已取消[/yellow]")
        return 130
    except Exception as e:
        console.print(f"\n[red]错误: {e}[/red]")
        return 1
    return 0


if (
    __name__ == "__main__"
    and 2 <= len(sys.argv) <= 3
    and sys.argv[1] in {"doctor", "check-config"}
    and sys.argv[2:] in ([], ["--strict"])
):
    raise SystemExit(run_bootstrap_doctor(strict="--strict" in sys.argv[2:]))

if __name__ == "__main__":
    raise SystemExit(main())
```
> `console` 在 `main()` 内构造，`--help`/bootstrap 路径不再 import rich。`except Exception` 分支仍打印 `错误: {e}`（保留现有文案），`logger.error(exc_info=...)` 若仍需可在此处 `from src.core.logger import setup_logger` 延迟导入。

- [ ] **Step 2: 运行确认通过 + 全量**

Run: `uv run pytest tests/test_cli_split.py tests/test_main.py -q`
Expected: PASS。
Run: `uv run pytest`
Expected: 全绿。
Run:
```bash
uv run python main.py --help
uv run python main.py doctor
```
Expected: `--help` 退出码 0、`usage:` 不变；`doctor` 退出码 0、文案不变。

- [ ] **Step 3: Commit**

```bash
git add main.py
git commit -m "refactor: reduce main.py to thin entrypoint with lazy imports"
```

---

## Self-Review

- **Spec coverage**：阶段 4 全部目标（拆分 main.py、list/doctor 独立、报告命令随 ReportRunner 收窄、顶层轻量）由 Task 1–4 覆盖；`_run_bootstrap_doctor` 语义保留（Task 2 `run_bootstrap_doctor`）；阶段 6 的 import 优化只做结构准备不做行为改变。
- **占位符**：`parser.py` 的"粘贴 build_parser 完整函数体"指向 `main.py` 现函数（明确指令，非占位符）；其余任务代码完整。
- **类型一致性**：报告 handler 注入的 `runner_factory` / `console` / `markdown` 签名在三个 handler 与测试间一致；request 字段名与 Plan 2 一致（`week_label` / `year_month` / `supplemental_input`）。
